skills:
  - name: navigate
    description: Plan and follow a path to a target location or pose.
    params:
      target_location:
        type: string
        source: [literal, blackboard]
        required: false
      pose:
        type: pose
        source: [literal, blackboard]
        required: false
      path:
        type: path
        source: [literal, blackboard]
        required: false
    outputs: [status, message, last_nav_result, battery_level]

  - name: pick
    description: Pick up an object at the current location. Constraint is single-arm robot; cannot pick something up while already holding something.
    params:
      object:
        type: string
        source: [literal, blackboard]
        required: false
      pose:
        type: pose
        source: [literal, blackboard]
        required: false
    outputs: [status, message, battery_level]

  - name: place
    description: Place the currently held object at the current location.
    params:
      pose:
        type: pose
        source: [literal, blackboard]
        required: false
    outputs: [status, message, battery_level]

  - name: detect
    description: Detect objects at the current location.
    params:
      object:
        type: string
        source: [literal, blackboard]
        required: false
    outputs: [status, message, detected_objects, battery_level]

  - name: open
    description: Open the current location or target location if provided. Constraint is single-arm robot; cannot open while holding an object.
    params:
      target_location:
        type: string
        source: [literal, blackboard]
        required: false
    outputs: [status, message, battery_level]

  - name: close
    description: Close the current location or target location if provided. Constraint is single-arm robot; cannot close while holding an object.
    params:
      target_location:
        type: string
        source: [literal, blackboard]
        required: false
    outputs: [status, message, battery_level]
